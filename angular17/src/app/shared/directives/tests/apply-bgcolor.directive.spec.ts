import { ApplyBgColorDirective } from '../apply-bgcolor.directive';

describe('ApplyBgcolorDirective', () => {
  it('should create an instance', () => {
    const directive = new ApplyBgColorDirective();
    expect(directive).toBeTruthy();
  });
});
