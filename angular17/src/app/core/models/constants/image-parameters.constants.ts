export const MAX_SIZE = 5 * 1024 * 1024; // 5MB
export const MIN_WIDTH = 300;
export const MIN_HEIGHT = 300;
export const INVALID_SIZE_MESSAGE = `Imagen supera el tamaño maximo de 5MB.`;
export const INVALID_DIMENSIONS_MESSAGE = `El tamaño mínimo para la imagen es de ${MIN_WIDTH}px x ${MIN_HEIGHT}px.`;
export const ERROR_LOAD_IMAGE_MEESSAGE = 'Error al cargar la imagen.';